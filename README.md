# Babysitter Kata (Completed)

## How to Build
- Requires maven
- `mvn package` builds the .jar and puts it into the `target` directory at the root of the project.
    - If there's a problem connect to the internet and run `mvn install`